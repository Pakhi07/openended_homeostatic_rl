diff --git a/modular_policy_sf.py b/modular_policy_sf.py
index 00cea9a..ae56cd3 100644
--- a/modular_policy_sf.py
+++ b/modular_policy_sf.py
@@ -139,11 +139,11 @@ class ModularActorCritic(ActorCritic):
             "module_logits": stacked_logits.float(),
             "module_values": stacked_values.float(),
         }
-        print({
-            "policy_logits": policy_logits.shape,
-            "values": values.shape,
-            "actions": actions.shape,
-            "log_prob_actions": log_prob_actions.shape,
-            "new_rnn_states": tuple(new_rnn_states.shape) if isinstance(new_rnn_states, torch.Tensor) else type(new_rnn_states),
-        })
-        return result
+        # print({
+        #     "policy_logits": policy_logits.shape,
+        #     "values": values.shape,
+        #     "actions": actions.shape,
+        #     "log_prob_actions": log_prob_actions.shape,
+        #     "new_rnn_states": tuple(new_rnn_states.shape) if isinstance(new_rnn_states, torch.Tensor) else type(new_rnn_states),
+        # })
+        return result
\ No newline at end of file
diff --git a/run_hcrafter_test_record.bash b/run_hcrafter_test_record.bash
index 7b838d9..540b409 100644
--- a/run_hcrafter_test_record.bash
+++ b/run_hcrafter_test_record.bash
@@ -1,6 +1,6 @@
 python enjoy_hcrafter_env_record.py \
 --env=HomeostaticCrafter-v1 \
---experiment=hcrafter_nmn_2 \
+--experiment=myhcrafter_500k \
 --train_dir=./train_dir \
 --device=cpu \
 --num_envs_per_worker=1 \
diff --git a/run_hcrafter_train.bash b/run_hcrafter_train.bash
index f5646e1..e528e16 100644
--- a/run_hcrafter_train.bash
+++ b/run_hcrafter_train.bash
@@ -1,15 +1,14 @@
 python train_hcrafter_env.py \
 --env=HomeostaticCrafter-v1 \
---experiment=myhcrafter \
+--experiment=myhcrafter_500k \
 --stats_avg=100 \
 --num_envs_per_worker=10 \
-# make above 2
 --with_wandb=False \
 --wandb_user=pakhibanchalia2418 \
 --wandb_project=homeostatic_crafter \
 --wandb_group=no_maxgrad_norm \
 --gamma=0.995 \
-# --train_for_env_steps=10000 \
+--train_for_env_steps=500000 \
 # --num_workers=1 \
 # --rollout=64 \
 # --batch_size=256 \
diff --git a/run_hcrafter_train_modular.bash b/run_hcrafter_train_modular.bash
index c01adc6..67dcbae 100644
--- a/run_hcrafter_train_modular.bash
+++ b/run_hcrafter_train_modular.bash
@@ -1,16 +1,12 @@
-python train_hcrafter_env.py \
+python train_hcrafter_env_modular.py \
 --env=HomeostaticCrafter-v1 \
---experiment=modular_debug_run \
+--experiment=modular_Aug27_1M \
 --stats_avg=100 \
 --num_envs_per_worker=2 \
 --with_wandb=False \
 --wandb_user=pakhibanchalia2418 \
---wandb_project=homeostatic_crafter \
+--wandb_project=homeostatic_crafter_modular \
 --wandb_group=no_maxgrad_norm \
 --gamma=0.995 \
---train_for_env_steps=10000 \
---num_workers=1 \
---rollout=64 \
---batch_size=256 \
---normalize_input=False \
-# --device=cpu
+--train_for_env_steps=1000000 \
+--batch_size=1024 \
diff --git a/train_hcrafter_env_modular.py b/train_hcrafter_env_modular.py
index 6fd73cb..dbb6b34 100644
--- a/train_hcrafter_env_modular.py
+++ b/train_hcrafter_env_modular.py
@@ -1,5 +1,6 @@
+# train_hcrafter_env_modular.py
+
 from typing import Optional
-import argparse
 import sys
 
 import gymnasium
@@ -10,13 +11,11 @@ from sample_factory.cfg.arguments import parse_full_cfg, parse_sf_args
 from sample_factory.envs.env_utils import register_env
 from sample_factory.train import run_rl
 
-from hcrafter_model import make_hcrafter_encoder
+# 1. Import the new modular encoder factory function
+from hcrafter_model_modular import make_modular_hcrafter_encoder
 from hcrafter_params import hcrafter_override_defaults
 from recorder import StatsRecorder, VideoRecorder, EpisodeRecorder
 
-# --- CHANGE 1: Import your new modular model ---
-from modular_policy_sf import ModularActorCritic
-
 
 def make_custom_env(
         full_env_name: str = 'HomeostaticCrafter-v1',
@@ -24,6 +23,8 @@ def make_custom_env(
         env_config=None,
         render_mode: Optional[str] = None,
 ):
+    # This function is now exactly as it was in the original non-modular script.
+    # No wrappers are needed.
     env = gymnasium.make(full_env_name, size=(64, 64), random_internal=True)
     return env
 
@@ -35,11 +36,11 @@ def make_custom_env_enjoy(
         render_mode: Optional[str] = None,
 ):
     env = gymnasium.make(full_env_name, size=(64, 64),
-                         random_internal=False,  # False by default!
+                         random_internal=False,
                          render_mode="human")
     
     from datetime import datetime
-    run_name = "enjoy_" + datetime.now().strftime("%Y-%m-%d-%H-%M-%S")
+    run_name = "enjoy_modular_" + datetime.now().strftime("%Y-%m-%d-%H-%M-%S")
     env = StatsRecorder(env, run_name)
     env = VideoRecorder(env, run_name, (512, 512))
     env = EpisodeRecorder(env, run_name)
@@ -53,27 +54,22 @@ def register_custom_env_envs(enjoy=False):
         register_env('HomeostaticCrafter-v1', make_custom_env)
 
 
-def register_hcrafter_models():
-    global_model_factory().register_encoder_factory(make_hcrafter_encoder)
-    # --- CHANGE 2: Register your custom ActorCritic model with sample-factory ---
-    global_model_factory().register_actor_critic_factory(ModularActorCritic)
+def register_modular_hcrafter_models():
+    # 2. Register our new modular encoder
+    global_model_factory().register_encoder_factory(make_modular_hcrafter_encoder)
 
 
 def parse_args(argv=None, evaluation=False):
     parser, partial_cfg = parse_sf_args(argv=argv, evaluation=evaluation)
-    
     hcrafter_override_defaults(parser)
-
-    # --- CHANGE 3: Add an argument to control the number of "selves" ---
-    parser.add_argument('--num_selves', default=4, type=int, help='Number of expert selves in the modular policy')
-    
     final_cfg = parse_full_cfg(parser, argv)
     return final_cfg
 
 
 def main():
     register_custom_env_envs()
-    register_hcrafter_models()
+    # 3. Call the function to register the modular model
+    register_modular_hcrafter_models()
     
     cfg = parse_args()
     
